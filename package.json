{
  "name": "ember-es6-class-codemod",
  "version": "0.1.0",
  "scripts": {
    "test": "codemod-cli test",
    "update-docs": "codemod-cli update-docs"
  },
  "bin": "./bin/cli.js",
  "keywords": [
    "codemod-cli"
  ],
  "dependencies": {
    "codemod-cli": "^0.1.0"
  },
  "devDependencies": {
    "jest": "^23.1.0"
  }
}
